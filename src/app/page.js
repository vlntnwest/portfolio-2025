import ProjectsWrapper from "@/components/Project/ProjectsWrapper";

const Home = () => {
  return (
    <main className="relative h-auto">
      <ProjectsWrapper />
    </main>
  );
};

export default Home;
